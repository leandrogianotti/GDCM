[build-system]
requires = ["scikit-build", "cmake", "ninja", "setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gdcm_tools"
version = "3.0.25"
description = "ProVoyance Shoulder GDCM tools x86_64"
authors = [
  { name = "Leandro Gianotti", email = "leandro.gianotti@intive.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = []